# 你让 AI 干活，然后呢？

你跟 OpenCode 说："帮我重构这个模块。"

它回："好的，我来处理。"

然后——

屏幕没动静。

你等着。

还是没动静。

你开始怀疑：它是在思考？在跑命令？在改文件？还是卡死了？

切到 OpenCode 窗口看一眼——哦，在跑。切回来继续干别的。过一会儿又忍不住切回去看……

**一天下来，光切窗口就切了几十次。**

不是因为 AI 不行。是因为它干活的过程对你不可见。

---

## 我做了个小东西

一个桌面悬浮挂件，圆形头像，常驻右上角。

它只做一件事：**实时告诉你 OpenCode 此刻在干啥。**

🟢 **绿环 + 呼吸光效** = 正在跑工具（命令、改文件…），下方显示具体动作

```
⚡ bash · git status
```

🟠 **橙环** = 在思考 / 生成文本

⚪ **灰环** = 空闲，挂件显示"摸鱼中 🐟"

你不用切窗口。扫一眼右上角，就知道它是在忙还是真挂了。

---

## 怎么做到的

OpenCode 把每次会话、每条消息、每个工具调用，都写进了本地一个 SQLite 文件里。

挂件**只读**打开这个文件，看最新一条记录的状态：

- 工具在跑 → 绿环
- 在推理 → 橙环
- 没动静 → 灰环

不碰 OpenCode 的一根毫毛，纯旁观。

---

## 不轮询，不浪费

本来挂件是每秒查一次数据库。但 agent 不动的时候也在查，白费 CPU。

后来发现：SQLite 每次写入都会动一个 WAL 文件。监听那个文件就行了——数据库一变，挂件才刷新；不写就不刷。

> 踩了个坑：macOS 的 FSEvents 文件监听抓不到 SQLite 的 mmap 写，盯了 5 秒一个事件都没有。换成底层的 VFS 事件源才行，一秒几十个 tick，跟 opencode 的写入完全同步。

agent 摸鱼的时候，挂件也摸鱼，零开销。

---

## 只显示真在干活的

第一版把所有会话都列出来，结果一堆"思考中"——其实几分钟前就结束的回合，被误判成活跃。

加了个时间窗：**只显示最近 20 秒内有活动的会话。**

结束了就自动消失。你看到的永远是"此刻正在发生的事"。

---

## 头像可以是你自己

默认 🤖。想换成自己的头像？扔一张 `avatar.png` 进去，重启就行，自动裁圆。

---

## 一个文件，零依赖

整个挂件就一个 `main.swift`，纯系统原生：

- SwiftUI 画界面
- SQLite3 读数据
- VFS 事件源驱动刷新
- NSWindow 做悬浮

不依赖 Electron，不依赖 Node，不用装任何东西。编译出来 170KB。

```bash
swiftc -O -lsqlite3 -framework AppKit -framework SwiftUI -o opencode-widget main.swift
```

一行命令，完事。

---

## 开源了

代码 + 演示视频（2 倍速）都在这：

👉 **gitee.com/fushoujiang/opencode-widget**

MIT，随便用。有 OpenCode + macOS 的同学直接能跑。

---

## 最后说一句

AI agent 最大的问题不是能力不够，是**过程不透明**。

你把活交给它了，但它干到哪了、卡没卡、要不要你介入——你全不知道。这种"不知道"很消耗注意力，比它干活本身还累。

挂件不能替你干活，但它能把那个"不知道"变成"一眼就知道"。

值了。
